// (2) Map
let w = ["alu", "bhaji"]
w.map(plate=>{console.log(plate);})


// (3) ForEach
let z = ["alu", "bhorta"]
z.map(bowl=>{console.log(bowl);})

// **Conclusion** The same result can be provided by 3 different "methods" (for, forEach & map) 
// **Note** Always use "map", because "forEach" allocates more memory